subir o banco

=== estando em src ===

go mod tidy
bazel mod tidy
bazel run //:gazelle
bazel build //cmd/main
bazel run //cmd/main

===========

Se você adicionar dependências Go novas ou mudar imports, rode de novo esta sequência mínima:

go mod tidy
bazel mod tidy
bazel run //:gazelle
